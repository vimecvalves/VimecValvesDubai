---
interface Props {
  title?: string;
  content?: string;
}
const { content, title } = Astro.props;
---

<div class="flex flex-col gap-2">
  <h1 class="reveal-up lg:text-5xl text-4xl font-medium">{title}</h1>
  {content && <p class="reveal-up max-w-[40ch]">{content}</p>}
</div>
